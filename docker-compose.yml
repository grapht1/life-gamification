services:
  life-gamification:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/data.sqlite
    volumes:
      - ./data:/data
    restart: unless-stopped
